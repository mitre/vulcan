-# - content_for :assets do
-#   = javascript_pack_tag 'icon'

%h1 Hello#projects
%p Find me in app/views/hello/projects.html.haml
.jumbotron
  %h1{:style => "t\"ext-align:center\""} Comment
  #commentbox.well{:style => "height:250px; border: solid 1px #222222; overflow-y: scroll"}
    - @messages.each do |message|
      = render 'messages/message', message: message
    -# <% @messages.each do |message|%>
    -#   <%= render 'messages/message', message: message %>
    -# <% end %>
= form_with model: @message do |f|
  .input-group.mb-3
    = f.text_field :body, placeholder: "comment", class: 'form-control'
    .input-group-append
      = f.submit "Send", class: "btn btn-secondary"
-# <%= form_with model: @message do |f| %>
-# <div class="input-group mb-3">
-#   <%= f.text_field :body, placeholder: "comment", class: 'form-control' %>
-#   <div class="input-group-append">
-#     <%= f.submit "Send", class: "btn btn-secondary" %>
-#   </div>
-# </div>
-# <% end %>