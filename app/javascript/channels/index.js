// Load all the channels within this directory and all subdirectories.
// Channel files must be named *_channel.js.

// Import consumer.js manually since we can't use require.context in ESM
import './consumer.js';
