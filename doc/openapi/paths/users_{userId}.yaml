parameters:
  - $ref: ../components/parameters/UserId.yaml
patch:
  operationId: patchUser
  tags:
    - Users
  summary: Partial update of user attributes (admin only)
  description: >-
    Partial update — send only changed fields. Updates a user's name,
    email, or admin status. Requires admin role. Prevents the last
    admin from demoting themselves. Sends a Slack notification when
    admin status changes (if Slack is configured). Returns both a
    toast and the updated user object.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            user:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
                admin:
                  type: boolean
        examples:
          promote:
            summary: Promote user to admin
            value:
              user:
                admin: true
  responses:
    '200':
      description: User updated
      content:
        application/json:
          schema:
            $ref: ../components/schemas/UserToastResponse.yaml
          examples:
            updated:
              summary: User promoted to admin
              value:
                toast:
                  title: User updated.
                  message:
                    - Successfully updated user.
                  variant: success
                user:
                  id: 42
                  name: Jane Doe
                  email: jane.doe@example.org
                  admin: true
    '422':
      description: Cannot remove last admin
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ToastResponse.yaml
          examples:
            last_admin:
              summary: Cannot demote last admin
              value:
                toast:
                  title: Cannot remove admin.
                  message:
                    - You are the only admin. Promote another user first.
                  variant: danger
    4XX:
      $ref: ../components/responses/ErrorResponse.yaml
put:
  operationId: updateUser
  tags:
    - Users
  summary: Full replacement of user attributes (admin only)
  description: >-
    Full replacement — all fields required. Updates a user's name,
    email, or admin status. Requires admin role. Prevents the last
    admin from demoting themselves. Sends a Slack notification when
    admin status changes (if Slack is configured). Returns both a
    toast and the updated user object.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            user:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
                admin:
                  type: boolean
        examples:
          promote:
            summary: Promote user to admin
            value:
              user:
                admin: true
  responses:
    '200':
      description: User updated
      content:
        application/json:
          schema:
            $ref: ../components/schemas/UserToastResponse.yaml
          examples:
            updated:
              summary: User promoted to admin
              value:
                toast:
                  title: User updated.
                  message:
                    - Successfully updated user.
                  variant: success
                user:
                  id: 42
                  name: Jane Doe
                  email: jane.doe@example.org
                  admin: true
    '422':
      description: Cannot remove last admin
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ToastResponse.yaml
          examples:
            last_admin:
              summary: Cannot demote last admin
              value:
                toast:
                  title: Cannot remove admin.
                  message:
                    - You are the only admin. Promote another user first.
                  variant: danger
    4XX:
      $ref: ../components/responses/ErrorResponse.yaml
delete:
  operationId: deleteUser
  tags:
    - Users
  summary: Delete a user (admin only)
  description: >-
    Permanently deletes a user account. Requires admin role. Cannot delete
    the last remaining site admin, nor a user who is the only admin of any
    project — transfer the admin role first. All project memberships and
    associated data are cleaned up via dependent destroy.
  responses:
    '200':
      description: User removed
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ToastResponse.yaml
          examples:
            removed:
              summary: User removed
              value:
                toast:
                  title: User removed.
                  message:
                    - Successfully removed user.
                  variant: success
    '422':
      description: >-
        Deletion blocked — the last site admin, or the only admin of one or
        more projects
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ToastResponse.yaml
          examples:
            last_site_admin:
              summary: Only site admin
              value:
                toast:
                  title: Cannot delete user.
                  message:
                    - This is the only admin. Promote another user first.
                  variant: danger
            sole_project_admin:
              summary: Only admin of a project
              value:
                toast:
                  title: Cannot delete user.
                  message:
                    - >-
                      This user is the only admin of: 'Photon OS 5'. Transfer
                      the admin role to another member of each project first.
                  variant: danger
    4XX:
      $ref: ../components/responses/ErrorResponse.yaml
