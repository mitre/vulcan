description: Validation errors
content:
  application/json:
    schema:
      type: object
      properties:
        toast:
          type: object
          properties:
            title:
              type: string
            message:
              type: array
              items:
                type: string
            variant:
              type: string
              example: danger
