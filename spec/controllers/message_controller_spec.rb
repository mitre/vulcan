require 'rails_helper'

RSpec.describe MessagesController  do
    context 'Creates message' do
        # it 'posts message to channel' do
        #     expect { post :create, message: { body: 'test' } }.to
        #     have_broadcasted_to("notifications_channel").with(body: 'test')
        # end
    end
end
