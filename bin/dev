#!/usr/bin/env sh

# Set the VULCAN_CONFIG environment variable to our default config
export VULCAN_CONFIG="/Users/alippold/github/mitre/vulcan/config/vulcan.default.yml"

# Default to port 3000 if not specified
export PORT="${PORT:-3000}"

# Run our custom dev command which uses concurrently
yarn dev
